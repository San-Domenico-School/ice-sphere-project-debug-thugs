using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FoxController : MonoBehaviour
{
    /// <summary>
    /// this is the fox Controller for the fox
    /// it is on the fox game object
    /// controlls animator and when to spawn
    /// </summary>
    // Start is called before the first frame update

 //   [SerializeField] private Rigidbody playerRB;
    //[SerializeField] private Vector3 PlayerPos;
   // private Transform FoxPosition;

    void Start()
    {
        //playerRB = GetComponent<Rigidbody>();

       // PlayerPos = playerRB.position;
    }
    void Update()
    {
       // float distance = Vector3.Distance(playerRB.position, .position);
    }

    //spawns the fox
    void SpawnFox()
    {
       //if (PlayerPos < 
    }
}

